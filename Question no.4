/* 4.Write a function to print first N natural numbers (TSRN) */
#include<stdio.h>
void natural(int n);
int main()
{
int n,k;
printf("Enter the number :");// input how many natural numbers you want to see,from user
scanf("%d",&n);
natural(n); // function calling

return 0;
}
void natural(int n){
// start loop counter from 1 and increment it by 1, and print in each iteration 
    for(int i=1;i<=n;i++){
        printf("%d\n",i);
    }
}
