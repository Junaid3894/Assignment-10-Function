/*3.Write a function to check whether a given number is even or odd. Return 1 if the
number is even, otherwise return 0. (TSRS)*/
#include<stdio.h>
int check(int n);
int main()
{
int n,k;
printf("Enter the number :");// input number to find even or odd ,from user
scanf("%d",&n);
k=check(n);
if(k==1) // if fuction return 1 then number is even otherwise odd
{
    printf("This number is even");
}
else
{
    printf("This number is odd");
}
return 0;
}
int check(int n){
    if(n%2==0)// check even condition before return
    {
        return 1;
    }
    else
    {
        return 0;
    }
}
